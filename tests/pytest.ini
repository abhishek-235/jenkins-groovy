[pytest]
markers =
    check_class: mark a test as a check_class when checking the class of a variable.
    e2e_test: marker for e2e test cases
    cleanup: mark a test as cleanup when all test cases executed and want to free resources.